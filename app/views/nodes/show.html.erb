<!-- app/views/nodes/show.html.erb -->

<p>
  <strong>Title:</strong>
  <%= @node.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @node.content %>
</p>

<%= link_to 'Edit', edit_node_path(@node) %> |
<%= link_to 'Back', nodes_path %>
<%= link_to 'Delete', @node, method: :delete, data: { turbo_confirm: "Are you sure?", turbo_method: :delete } %>
